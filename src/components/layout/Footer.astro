---
import { footerConfig, siteConfig } from '@/site-config'

const year = new Date().getFullYear()

const SocialMediaIconId: Record<string, string> = {
  coolapk: 'coolapk-line',
  telegram: 'mingcute-telegram-line',
  github: 'mingcute-github-line',
  bilibili: 'ri-bilibili-line',
  twitter: 'mingcute-twitter-line',
  zhihu: 'ri-zhihu-line',
  steam: 'ri-steam-line',
  netease_music: 'ri-netease-cloud-music-line',
  mail: 'mingcute-mail-line'
}
---

<footer class='mx-auto mt-24 w-full'>
  <div class='border-t border-border pt-5'>
    <div
      class='flex flex-col items-start gap-y-3 sm:flex sm:flex-row sm:items-start sm:justify-between sm:gap-y-0'
    >
      <div class='flex flex-col items-start gap-2 text-sm sm:flex-row sm:gap-x-4'>
        <div class='text-sm text-muted-foreground [&_a:hover]:text-primary [&_a]:text-foreground'>
          © 2024 - {year}
          {siteConfig.author}
        </div>
        <a href='/terms' class='text-muted-foreground'>Site Policy</a>
        <div class='pageview-counter-wrapper relative text-muted-foreground'>
          <span class='pageview-counter'>
            Home <span class='waline-pageview-count' data-counter-label='Home views' data-path='/'
              >-</span
            >
            <span class='mx-1 text-muted-foreground/70'>|</span>
            Site <span
              id='total-pageview-count'
              class='transition-colors'
              title='Click to refresh (all site pages)'
              role='button'
              tabindex='0'
              aria-live='polite'
              aria-describedby='pageview-counter-tooltip'
              aria-label='All site pages pageviews. Click or press Enter or Space to refresh.'
              >-</span
            >
          </span>
          <span
            id='pageview-counter-tooltip'
            class='pageview-tooltip pointer-events-none absolute bottom-full left-1/2 mb-2 hidden -translate-x-1/2 whitespace-nowrap rounded-lg border border-border bg-popover px-3 py-2 text-xs text-popover-foreground shadow-sm'
            role='tooltip'
          >
            Home: homepage visits. Site: all page visits.
          </span>
        </div>
        {
          footerConfig.registration && (
            <div class='text-sm text-muted-foreground'>
              <a class='hover:text-primary' href={footerConfig.registration.url} target='_blank'>
                {footerConfig.registration.text}
              </a>
            </div>
          )
        }
        {
          footerConfig.mengICP && (
            <div class='text-sm text-muted-foreground'>
              <a class='hover:text-primary' href={footerConfig.mengICP.url} target='_blank'>
                {footerConfig.mengICP.text}
              </a>
            </div>
          )
        }
      </div>
      <div class='flex items-center justify-between'>
        {/* Social Brands */}
        <div class='flex items-center gap-x-4'>
          {
            footerConfig.socialLinks.map((platform: { name: string; url: string }) => (
              <a
                class='inline-block text-muted-foreground transition-all hover:text-primary'
                href={platform.url}
                aria-label={platform.name}
              >
                <svg class='size-6'>
                  <use href={`/icons/social.svg#${SocialMediaIconId[platform.name]}`} />
                </svg>
              </a>
            ))
          }
          {/* Rss */}
          <a
            class='inline-block text-muted-foreground transition-all hover:text-primary'
            href='/rss.xml'
            aria-label='rss'
          >
            <svg class='size-6'>
              <use href='/icons/social.svg#mingcute-rss-2-line'></use>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>

<script>
  /**
   * Lazy-load footer pageview summary after the page is idle.
   *
   * Parameters
   * ----------
   * None
   *
   * Returns
   * -------
   * void, shape=(), dtype=undefined
   *     Imports the pageview client module and initializes the footer counters.
   */
  const init_footer_pageview = () => {
    import('@/utils/pageview.js')
      .then((module) => {
        if (typeof module.initPageviewCounter === 'function') {
          module.initPageviewCounter()
        }
      })
      .catch((err) => console.warn('Pageview script load failed:', err))
  }

  if (typeof window.requestIdleCallback === 'function') {
    window.requestIdleCallback(init_footer_pageview, { timeout: 3000 })
  } else {
    window.addEventListener('load', () => setTimeout(init_footer_pageview, 300), { once: true })
  }
</script>

<style>
  #total-pageview-count {
    cursor: pointer;
  }

  #total-pageview-count:hover,
  #total-pageview-count:focus-visible {
    color: hsl(var(--primary));
  }

  #total-pageview-count[title*='Partially loaded'] {
    color: hsl(var(--destructive));
  }

  .pageview-counter-wrapper:hover .pageview-tooltip,
  .pageview-counter-wrapper:focus-within .pageview-tooltip {
    display: inline-block;
  }
</style>
