---
import { cn } from '@/utils'

const { as: Tag = 'a', class: className, title, href, style = 'button', ...props } = Astro.props
const isTextArrow = style === 'ahead' || style === 'previous'
---

<Tag
  class={cn(
    'group/button inline-flex items-center gap-x-1 text-sm transition-colors',
    isTextArrow
      ? 'min-h-10 w-fit gap-2 font-medium text-foreground hover:text-primary'
      : 'min-h-10 rounded-lg border border-border/60 px-3 py-2 text-muted-foreground hover:border-border hover:bg-muted/50',
    className,
    Tag === 'a' && !href && 'cursor-default',
    style === 'pill' && 'rounded-full'
  )}
  href={href}
  {...props}
>
  <slot name='before'>
    {
      style === 'previous' && (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width='16'
          height='16'
          viewBox='0 0 24 24'
          fill='none'
          stroke-width='2.5'
          stroke-linecap='round'
          stroke-linejoin='round'
          class='rotate-180 stroke-current'
        >
          <line
            x1='5'
            y1='12'
            x2='19'
            y2='12'
            class='translate-x-4 scale-x-0 transition-transform duration-300 ease-in-out group-hover/button:translate-x-1 group-hover/button:scale-x-100'
          />
          <polyline
            points='12 5 19 12 12 19'
            class='translate-x-0 transition-transform duration-300 ease-in-out group-hover/button:translate-x-1'
          />
        </svg>
      )
    }
  </slot>
  <slot>
    {
      title &&
        (isTextArrow ? (
          <span class='relative after:absolute after:-bottom-1 after:left-0 after:h-px after:w-full after:origin-left after:scale-x-0 after:bg-current after:transition-transform after:duration-300 group-hover/button:after:scale-x-100'>
            {title}
          </span>
        ) : (
          <span>{title}</span>
        ))
    }
  </slot>
  <slot name='after'>
    {
      style === 'ahead' && (
        <svg
          xmlns='http://www.w3.org/2000/svg'
          width='16'
          height='16'
          viewBox='0 0 24 24'
          fill='none'
          stroke-width='2.5'
          stroke-linecap='round'
          stroke-linejoin='round'
          class='stroke-current'
        >
          <line
            x1='5'
            y1='12'
            x2='19'
            y2='12'
            class='translate-x-4 scale-x-0 transition-transform duration-300 ease-in-out group-hover/button:translate-x-1 group-hover/button:scale-x-100'
          />
          <polyline
            points='12 5 19 12 12 19'
            class='translate-x-0 transition-transform duration-300 ease-in-out group-hover/button:translate-x-1'
          />
        </svg>
      )
    }
  </slot>
</Tag>
