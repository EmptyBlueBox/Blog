---
import { cn } from '@/utils'

interface Props {
  class?: string
  sponsors: {
    name: string
    amount: string
    date: string
  }[]
}
const { class: className, sponsors, ...props } = Astro.props
---

<div class={cn('mx-auto w-fit max-w-full overflow-hidden', className)} {...props}>
  <table class='my-0 -mb-px w-full table-auto'>
    <thead>
      <tr class='border-y border-gray-200 bg-slate-100 dark:border-gray-700 dark:bg-slate-800'>
        <th
          class='px-4 py-3 text-center font-medium text-gray-700 dark:text-gray-300 md:px-6 lg:px-8'
          >Name</th
        >
        <th
          class='px-4 py-3 text-center font-medium text-gray-700 dark:text-gray-300 md:px-6 lg:px-8'
          >Amount</th
        >
        <th
          class='px-4 py-3 text-center font-medium text-gray-700 dark:text-gray-300 md:px-6 lg:px-8'
          >Date</th
        >
      </tr>
    </thead>
    <tbody>
      {
        sponsors.map((sponsor) => (
          <tr class='border-b border-gray-200 hover:bg-slate-50 dark:border-gray-700 dark:hover:bg-slate-700'>
            <td class='px-4 py-3 text-center text-gray-600 dark:text-gray-400 md:px-6 lg:px-8'>
              {sponsor.name}
            </td>
            <td class='px-4 py-3 text-center text-gray-600 dark:text-gray-400 md:px-6 lg:px-8'>
              {sponsor.amount}
            </td>
            <td class='px-4 py-3 text-center text-gray-600 dark:text-gray-400 md:px-6 lg:px-8'>
              {sponsor.date}
            </td>
          </tr>
        ))
      }
    </tbody>
  </table>
</div>
